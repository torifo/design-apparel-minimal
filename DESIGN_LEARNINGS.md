# FORM Design Learnings

## 目的

minimalペルソナは、装飾の少なさだけでなく、削った結果として何が残るかを見ている。FORMでは、余白、線、寸法、素材情報だけでブランドの方向性を伝える設計にした。

このブランドは架空ブランドであり、実在の店舗・商品ではない。

## 設計したこと

- ヒーローに写真を置かず、巨大なFORMロゴと短い思想だけで始める。
- 寸法線と `shoulder 48` `hem 62` `wool 92` の測定値を入れ、余白が空白ではなく設計図に見えるようにした。
- 商品グリッドは完全な等幅にし、発見感ではなく判断しやすさを優先した。
- 唯一の強い転換として黒背景のPhilosophyを置き、余白の中に緊張点を作った。

## CSS

主要なCSS判断:

```css
:root {
  --bg: #FAFAFA;
  --bg-dark: #111111;
  --fg: #111111;
  --border: #E0E0E0;
  --font-display: 'Bebas Neue', sans-serif;
  --font-body: 'Outfit', 'Noto Sans JP', sans-serif;
}

.hero::before,
.hero::after {
  background: var(--border);
}

.collection-grid {
  grid-template-columns: repeat(4, 1fr);
  gap: 1px;
}
```

装飾要素は1px線、白、黒、グレーに限定した。視認性のため、薄い文字には色を変えずにごく弱い `text-stroke` を入れている。

## アニメーション

- ページ全体は0.6秒のopacityのみで表示。
- revealもopacityのみ。translateは使わない。
- 商品ホバーは `scale(1.02)` のみ。

FORMでは動きが強いほど思想が弱くなるため、アニメーションは存在を主張しない範囲に制限した。

## フォント

- Display: `Bebas Neue`
- Body: `Outfit`

Bebas Neueは大文字の建築的な骨格を作るために使った。Outfitは幾何学的で静かな本文に向いており、余白の中でもノイズになりにくい。

## ボタン

FORMではファーストビューにCTAボタンを置いていない。購入誘導よりも、ブランドの姿勢を理解することを優先した。リンク類もテキストのみで、アイコンや装飾を排除している。

## UI/UX

- ナビは1pxボーダーとテキストだけで構成。
- 素材情報は`dl`で構造化し、産地・特性を並列に読めるようにした。
- モバイルでは2カラム商品グリッドを維持し、ミニマルな緊張感を保つ。

## レイアウト

大きな余白、水平線、垂直線、等幅グリッドで構成した。グリッドを崩さないことで、他3ブランドとの違いを明確にしている。余白は飾りではなく、構造と素材を見るための間として扱っている。

